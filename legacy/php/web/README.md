# Legacy PHP UI/API

This directory is retired and is not used by the current irrigation controller.

Current stack:
- Python daemon
- Python HTTP gateway
- Flutter UI

Do not deploy or expose these PHP files.
